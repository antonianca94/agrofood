<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    
    <link href="/public/css/bootstrap.css" rel="stylesheet" />
    <link href="/public/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="/public/css/agrofood.css" rel="stylesheet" />
</head>

<body class="vendor_order_details_page">

    <%- include('../../header', { user: user }) %>
    <div class="main-content d-xxl-flex d-xl-flex d-lg-flex"> 
        <%- include('../../sidebar', { pageType: 'Vendas', userRole }) %>
        <div class="container-fluid mt-5 mb-5 m-xxl-4 m-xl-4 m-lg-4 m-md-4 m-sm-4">
            <!-- Breadcrumb / Voltar -->
            <div class="row mb-3">
                <div class="col-12">
                    <a href="/vendas" class="text-decoration-none">
                        <i class="bi bi-arrow-left me-2"></i> Voltar para Minhas Vendas
                    </a>
                </div>
            </div>

            <!-- Header do Pedido -->
            <div class="row mb-4">
                <div class="col-12">
                    <div class="card p-4">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col-md-6">
                                    <h4 class="mb-2">
                                        <i class="bi bi-receipt c-orange me-2"></i>
                                        Venda #<%= order.order_number %>
                                    </h4>
                                    <p class="mb-0 text-muted">
                                        Recebido em <span class="c-orange"><%= order.createdAtFormatted %></span>
                                    </p>
                                </div>
                                <div class="col-md-6 text-md-end mt-3 mt-md-0">
                                    <% 
                                    let badgeClass = 'bg-secondary';
                                    switch(order.status) {
                                        case 'pending':
                                            badgeClass = 'bg-warning';
                                            break;
                                        case 'processing':
                                            badgeClass = 'bg-info';
                                            break;
                                        case 'shipped':
                                            badgeClass = 'bg-primary';
                                            break;
                                        case 'delivered':
                                            badgeClass = 'bg-success';
                                            break;
                                        case 'cancelled':
                                            badgeClass = 'bg-danger';
                                            break;
                                    }
                                    %>
                                    <span class="badge <%= badgeClass %> fs-6 px-3 py-2">
                                        <%= order.statusFormatted %>
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <!-- Coluna Esquerda - Produtos e Informações -->
                <div class="col-lg-8">
                    
                    <!-- Produtos do Pedido -->
                    <div class="card p-4 mb-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-box-seam c-orange me-2"></i>
                                Produtos da Venda
                            </h5>
                        </div>
                        <div class="card-body">
                            <% if (items && items.length > 0) { %>
                                <% items.forEach((item, index) => { %>
                                    <div class="row align-items-center py-3 <%= index < items.length - 1 ? 'border-bottom' : '' %>">
                                        <div class="col-md-2 text-center mb-3 mb-md-0">
                                            <div class="product-image-container">
                                                <% if (item.featuredImage) { %>
                                                    <img 
                                                        src="<%= item.featuredImage %>" 
                                                        alt="<%= item.product_name %>" 
                                                        class="product-image"
                                                    >
                                                <% } else { %>
                                                    <div class="product-image-placeholder">
                                                        <i class="bi bi-image fs-1 text-muted"></i>
                                                    </div>
                                                <% } %>
                                            </div>
                                        </div>
                                        <div class="col-md-5 mb-3 mb-md-0">
                                            <h6 class="mb-1"><%= item.product_name %></h6>
                                            <% if (item.sku) { %>
                                                <small class="text-muted">
                                                    <i class="bi bi-upc"></i> SKU: <%= item.sku %>
                                                </small>
                                            <% } %>
                                        </div>
                                        <div class="col-md-2 text-center mb-3 mb-md-0">
                                            <small class="text-muted d-block">Quantidade</small>
                                            <strong class="fs-5"><%= item.quantity %></strong>
                                        </div>
                                        <div class="col-md-3 text-md-end">
                                            <small class="text-muted d-block">Subtotal</small>
                                            <strong class="c-orange fs-5"><%= item.subtotalFormatted %></strong>
                                            <br>
                                            <small class="text-muted"><%= item.priceFormatted %> cada</small>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <p class="text-muted mb-0">Nenhum produto encontrado nesta venda.</p>
                            <% } %>
                        </div>
                    </div>

                    <!-- Informações do Comprador -->
                    <% if (buyer) { %>
                    <div class="card p-4 mb-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-person-circle c-orange me-2"></i>
                                Informações do Comprador
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6 mb-3 mb-md-0">
                                    <small class="text-muted d-block">Nome</small>
                                    <strong class="c-orange"><%= buyer.name || 'Nome não informado' %></strong>
                                </div>
                                <% if (buyer.phone) { %>
                                <div class="col-md-6">
                                    <small class="text-muted d-block">Telefone</small>
                                    <a href="tel:<%= buyer.phone %>"><%= buyer.phone %></a>
                                </div>
                                <% } %>
                            </div>
                            <% if (buyer.email) { %>
                            <div class="row mt-3">
                                <div class="col-md-6">
                                    <small class="text-muted d-block">E-mail</small>
                                    <a href="mailto:<%= buyer.email %>"><%= buyer.email %></a>
                                </div>
                            </div>
                            <% } %>
                            <% if (buyer.document) { %>
                            <div class="row mt-3">
                                <div class="col-md-4">
                                    <small class="text-muted d-block">CPF/CNPJ</small>
                                    <strong><%= buyer.document %></strong>
                                </div>
                            </div>
                            <% } %>
                        </div>
                    </div>
                    <% } %>

                    <!-- Timeline do Pedido -->
                    <div class="card p-4 mb-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-clock-history c-orange me-2"></i>
                                Histórico da Venda
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="timeline">
                                <div class="timeline-item <%= order.status === 'pending' || order.status === 'processing' || order.status === 'shipped' || order.status === 'delivered' ? 'active' : '' %>">
                                    <div class="timeline-marker"></div>
                                    <div class="timeline-content">
                                        <h6>Pedido Recebido</h6>
                                        <p class="text-muted mb-0"><%= order.createdAtFormatted %></p>
                                    </div>
                                </div>

                                <div class="timeline-item <%= order.status === 'processing' || order.status === 'shipped' || order.status === 'delivered' ? 'active' : '' %>">
                                    <div class="timeline-marker"></div>
                                    <div class="timeline-content">
                                        <h6>Em Processamento</h6>
                                        <p class="text-muted mb-0">Venda aceita e sendo preparada</p>
                                    </div>
                                </div>

                                <div class="timeline-item <%= order.status === 'shipped' || order.status === 'delivered' ? 'active' : '' %>">
                                    <div class="timeline-marker"></div>
                                    <div class="timeline-content">
                                        <h6>Enviado</h6>
                                        <p class="text-muted mb-0">Venda despachado para entrega</p>
                                    </div>
                                </div>

                                <div class="timeline-item <%= order.status === 'delivered' ? 'active' : '' %>">
                                    <div class="timeline-marker"></div>
                                    <div class="timeline-content">
                                        <h6>Entregue</h6>
                                        <p class="text-muted mb-0">Venda entregue ao cliente</p>
                                    </div>
                                </div>

                                <% if (order.status === 'cancelled') { %>
                                <div class="timeline-item cancelled">
                                    <div class="timeline-marker"></div>
                                    <div class="timeline-content">
                                        <h6>Cancelado</h6>
                                        <p class="text-muted mb-0">Este venda foi cancelado</p>
                                    </div>
                                </div>
                                <% } %>
                            </div>
                        </div>
                    </div>

                </div>

                <!-- Coluna Direita - Resumo e Ações -->
                <div class="col-lg-4">
                    
                    <!-- Resumo do Pedido -->
                    <div class="card p-4 mb-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-receipt-cutoff c-orange me-2"></i>
                                Resumo da Venda
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="d-flex justify-content-between mb-2">
                                <span>Total da Venda</span>
                                <strong class="c-orange fs-4"><%= order.totalFormatted %></strong>
                            </div>
                            <hr>
                            <div class="d-flex justify-content-between mb-2">
                                <small class="text-muted">Forma de Pagamento</small>
                                <small class="c-orange fw-bold"><%= order.paymentMethodFormatted %></small>
                            </div>
                        </div>
                    </div>

                    <!-- Endereço de Entrega -->
                    <div class="card p-4 mb-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-geo-alt-fill c-orange me-2"></i>
                                Endereço de Entrega
                            </h5>
                        </div>
                        <div class="card-body">
                            <p class="mb-1"><strong><%= order.shipping_address %></strong></p>
                            <p class="mb-1"><%= order.shipping_city %>/<%= order.shipping_state %></p>
                            <p class="mb-0">CEP: <%= order.shipping_cep %></p>
                            <% if (order.shipping_complement) { %>
                            <p class="mt-2 mb-0">
                                <small class="text-muted">Complemento:</small>
                                <%= order.shipping_complement %>
                            </p>
                            <% } %>
                        </div>
                    </div>

                    <!-- Ações do Vendedor -->
                    <div class="card p-4">
                        <div class="card-header bg-white">
                            <h5 class="mb-0">
                                <i class="bi bi-gear-fill c-orange me-2"></i>
                                Gerenciar Venda
                            </h5>
                        </div>
                        <div class="card-body">
                            <% if (order.status === 'pending') { %>
                            <button class="btn btn-success w-100 mb-2" onclick="updateStatus('<%= order.id %>', 'processing')">
                                <i class="bi bi-check-circle me-2"></i>
                                Aceitar Venda
                            </button>
                            <button class="btn btn-danger w-100" onclick="updateStatus('<%= order.id %>', 'cancelled')">
                                <i class="bi bi-x-circle me-2"></i>
                                Recusar Venda
                            </button>
                            <% } %>

                            <% if (order.status === 'processing') { %>
                            <button class="btn btn-primary w-100 mb-2" onclick="updateStatus('<%= order.id %>', 'shipped')">
                                <i class="bi bi-truck me-2"></i>
                                Marcar como Enviado
                            </button>
                            <button class="btn btn-outline-danger w-100" onclick="updateStatus('<%= order.id %>', 'cancelled')">
                                <i class="bi bi-x-circle me-2"></i>
                                Cancelar Venda
                            </button>
                            <% } %>

                            <% if (order.status === 'shipped') { %>
                            <button class="btn btn-success w-100" onclick="updateStatus('<%= order.id %>', 'delivered')">
                                <i class="bi bi-check-circle-fill me-2"></i>
                                Marcar como Entregue
                            </button>
                            <% } %>

                            <% if (order.status === 'delivered' || order.status === 'cancelled') { %>
                            <div class="alert alert-info mb-0">
                                <i class="bi bi-info-circle me-2"></i>
                                Este pedido já foi finalizado.
                            </div>
                            <% } %>

                            <hr class="my-3">
                            
                            <button class="btn btn-outline-secondary btn-sm w-100 mb-2" onclick="window.print()">
                                <i class="bi bi-printer me-2"></i>
                                Imprimir Venda
                            </button>

                            <% if (buyer && buyer.email) { %>
                            <a href="mailto:<%= buyer.email %>" class="btn btn-outline-secondary btn-sm w-100">
                                <i class="bi bi-envelope me-2"></i>
                                Contatar Cliente
                            </a>
                            <% } %>
                        </div>
                    </div>

                </div>
            </div>

            <!-- FOOTER -->
            <div class="footer">
                <%- include('../../footer') %>
            </div>
            <!-- FOOTER -->
        </div>
    </div>

    <script>
        function updateStatus(orderId, newStatus) {
            const messages = {
                'processing': 'Deseja aceitar este pedido?',
                'shipped': 'Confirmar que o pedido foi enviado?',
                'delivered': 'Confirmar que o pedido foi entregue?',
                'cancelled': 'Tem certeza que deseja cancelar este pedido?'
            };

            if (!confirm(messages[newStatus] || 'Deseja atualizar o status?')) {
                return;
            }

            fetch(`/compra/${orderId}/status`, {
                method: 'PATCH',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ status: newStatus })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('Status atualizado com sucesso!');
                    window.location.reload();
                } else {
                    alert('Erro ao atualizar status: ' + (data.error || 'Tente novamente'));
                }
            })
            .catch(error => {
                console.error('Erro:', error);
                alert('Erro ao atualizar status');
            });
        }
    </script>

    <style>
        .product-image-container {
            width: 100px;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            overflow: hidden;
            background-color: #f8f9fa;
        }

        .product-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
        }

        .product-image-placeholder {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .timeline {
            position: relative;
            padding-left: 30px;
        }

        .timeline-item {
            position: relative;
            padding-bottom: 30px;
            opacity: 0.5;
        }

        .timeline-item.active {
            opacity: 1;
        }

        .timeline-item.cancelled {
            opacity: 1;
        }

        .timeline-item .timeline-marker {
            position: absolute;
            left: -23px;
            top: 5px;
            width: 15px;
            height: 15px;
            border-radius: 50%;
            background: #e0e0e0;
            border: 3px solid white;
            box-shadow: 0 0 0 2px #e0e0e0;
        }

        .timeline-item.active .timeline-marker {
            background: #d96900;
            box-shadow: 0 0 0 2px #d96900;
        }

        .timeline-item.cancelled .timeline-marker {
            background: #dc3545;
            box-shadow: 0 0 0 2px #dc3545;
        }

        .timeline-item:after {
            content: '';
            position: absolute;
            left: -16px;
            top: 20px;
            width: 2px;
            height: calc(100% - 15px);
            background: #e0e0e0;
        }

        .timeline-item:last-child:after {
            display: none;
        }

        .timeline-item.active:after {
            background: #d96900;
        }

        .timeline-content h6 {
            font-size: 1rem;
            margin-bottom: 5px;
        }

        .timeline-content p {
            font-size: 0.875rem;
        }

        .c-orange {
            color: #d96900;
        }

        @media print {
            .sidebar, .footer, .btn, nav, .breadcrumb {
                display: none !important;
            }
            .main-content {
                margin: 0 !important;
                padding: 0 !important;
            }
        }
    </style>

</body>
</html>